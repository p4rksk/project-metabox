{{> layout/header}}
<link rel="stylesheet" href="/css/mypageDetailSaw.css">

<div class="container">
    <!-- 유저 정보 -->
    <div class="row mt-4">
        {{> layout/mypage-detail-sidebar}}

        <!--영화 리스트-->
        <div class="col-md-9">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4>내가 본 영화 {{ticketedList.tickedCount}}건</h4>
                <div class="d-flex">
                    <select class="form-control mr-2" id="yearSelect">
                        <option value="">년도 선택</option>
                        <option value="2023">2023</option>
                        <option value="2024">2024</option>
                    </select>
                    <button class="btn btn-outline-danger form-control" onclick="searchMovies()">검색</button>
                </div>
            </div>
            {{#ticketedList.tickets}}
                <div class="movie-card">
                    <img src="/image/movie/{{imgFilename}}" class="movie-poster" alt="Exhuma">
                    <div class="movie-info">
                        <h5>{{title}}</h5>
                        <p>관람일시 {{date}} ({{startTime}} ~ {{endTime}})</p>
                        <p>관람극장 {{theaterName}} ({{name}})</p>
                        <p>예매번호 {{bookNum}}</p>
                    </div>
                </div>
            {{/ticketedList.tickets}}
            {{^ticketedList.tickets}}
                <p>고객님의 관람 내역이 존재하지 않습니다.</p>
            {{/ticketedList.tickets}}
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script>
    function searchMovies() {
        const selectedYear = document.getElementById('yearSelect').value;
        if (selectedYear) {
            alert(`선택한 년도: ${selectedYear}`);
            // 여기에 선택한 년도에 따라 검색 로직을 구현하세요
        }
    }
</script>

{{> layout/footer}}